import React from 'react';
import Banner from '../Banner/Banner';
import Nav from '../Nav/Nav';

const Header = () => {
    return (
        <div>
            This is header
            <Nav />
            <Banner />
        </div>
    );
};

export default Header;